# =============================================================================
# BITTENSOR SUBNET CONFIGURATION
# =============================================================================

# Subnet ID - Update this based on network:
# Testnet: Usually different ID (check current testnet subnet)
# Mainnet: *** (current production subnet ID)
NETUID=

# =============================================================================
# WALLET CONFIGURATION  
# =============================================================================

# Wallet names created with btcli
# Example: btcli wallet create --wallet.name miner --wallet.hotkey default
WALLET_NAME=miner
WALLET_HOTKEY=default

# =============================================================================
# NETWORK CONFIGURATION
# =============================================================================

# Network type: 'test' for testnet, 'finney' for mainnet
SUBTENSOR_NETWORK=finney

# Chain endpoint URLs:
# Mainnet: wss://entrypoint-finney.opentensor.ai:443
# Testnet: wss://test.finney.opentensor.ai:443
SUBTENSOR_CHAIN_ENDPOINT=wss://entrypoint-finney.opentensor.ai:443

# =============================================================================
# MINER CONFIGURATION
# =============================================================================

# Axon port for miner communication (default: 8091)
# Each miner on same server should use different port
AXON_PORT=8091

# =============================================================================
# BLACKLIST CONFIGURATION
# =============================================================================

# Force validator permit (True/False)
# True: Only allow validators with permits to query this miner
BLACKLIST_FORCE_VALIDATOR_PERMIT=True

# Minimum validator stake required (in TAO)
# Validators with less stake will be blacklisted
BLACKLIST_VALIDATOR_MIN_STAKE=1000